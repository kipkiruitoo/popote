class SubService{
  String title;
  String description, cost, timelines, contact;
  int serviceId;

  SubService({this.title, this.description, this.cost, this.contact, this.timelines, this.serviceId});

}